#!/usr/bin/env python3
"""Source-checkout launcher. Installed entry points use the same main()."""
from pathlib import Path
import sys

sys.path.insert(0, str(Path(__file__).resolve().parents[1] / "src"))
from verantyx.cli import main

raise SystemExit(main())
