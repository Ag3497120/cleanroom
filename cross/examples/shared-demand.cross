# 文の順番ではなく接続から動く。二つの入力辺が同じ共有計算につながる。
プロジェクト「要求・共有・合流」。
「合流」は「左の処理」と「右の処理」を足す。
「左の処理」は「共有計算」と数「2」を足す。
「右の処理」は「共有計算」と数「3」を掛ける。
「共有計算」は数「12」と数「4」を掛ける。
「条件」は数「12」が数「20」以下か。
「使わない循環」は「使わない循環」。
「結果」は「条件」なら「合流」、そうでなければ「使わない循環」を選ぶ。
出力は「結果」。
